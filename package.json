{
  "name": "zed_bot",
  "version": "0.1.0",
  "private": true,
  "description": "ZED_BOT - production-ready Telegram VPN sales bot (infrastructure foundation)",
  "repository": {
    "type": "git",
    "url": "https://github.com/Mhoseinshah1/ZED_BOT.git"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}
