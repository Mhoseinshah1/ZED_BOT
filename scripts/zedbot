#!/usr/bin/env bash
# Compatibility wrapper - the canonical CLI lives in scripts/zedbot.sh
# (installed to /usr/local/bin/zedbot by scripts/install.sh).
set -Eeuo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
exec bash "${SCRIPT_DIR}/zedbot.sh" "$@"
