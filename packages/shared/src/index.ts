// Shared types, constants and utilities used across ZED_BOT services.
// Placeholder - grows alongside the real features.

export const APP_NAME = "ZED_BOT";

export interface HealthStatus {
  ok: boolean;
  service: string;
}
